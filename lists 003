nList = list()

while True:
    for i in range(3):
        names = input("Enter names of people you want to invite---> ")
        nList.append(names)

    loop = input("Invite more? yes or no ").lower()

    if loop == "no":
        break
print(str(len(nList)) + " people invited")
